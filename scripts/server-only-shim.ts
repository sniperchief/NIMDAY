// No-op stand-in for Next.js's `server-only` marker, used when running project
// code outside the Next runtime (the worker and the dev scripts). The real
// package only exists to fail a *client* bundle, which never applies here.
export {};
